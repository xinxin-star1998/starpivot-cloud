<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.org.starpivot.mall.wms.mapper.WmsWareOrderTaskDetailMapper">

    <resultMap type="cn.org.starpivot.mall.wms.entity.WmsWareOrderTaskDetail" id="WmsWareOrderTaskDetailResult">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="skuName" column="sku_name"/>
        <result property="skuNum" column="sku_num"/>
        <result property="taskId" column="task_id"/>
        <result property="wareId" column="ware_id"/>
        <result property="lockStatus" column="lock_status"/>
    </resultMap>

    <sql id="selectWareOrderTaskDetailVo">
        select id, sku_id, sku_name, sku_num, task_id, ware_id, lock_status
        from wms_ware_order_task_detail
    </sql>

    <select id="listByTaskId" resultMap="WmsWareOrderTaskDetailResult">
        <include refid="selectWareOrderTaskDetailVo"/>
        where task_id = #{taskId}
        order by id
    </select>
</mapper>
