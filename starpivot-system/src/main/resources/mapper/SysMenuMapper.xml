<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.org.starpivot.system.mapper.SysMenuMapper">
    <select id="getMenuByRoleId" resultType="cn.org.starpivot.system.domain.entity.SysMenu">
        select m.* from sys_menu m
        inner join sys_role_menu rm on m.menu_id = rm.menu_id
        where rm.role_id = #{roleId} and m.status = '0' and m.visible = '0'
        order by m.order_num
    </select>
    <select id="selectMenuIds" resultType="java.lang.Long">
        select menu_id from sys_menu where status = '0'
    </select>
    <select id="selectPermissionsByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_menu m
                 INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
                 INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.status = '0'
          AND m.perms IS NOT NULL
          AND m.perms &lt;&gt; ''
    </select>
    <select id="selectAllPermissionStrings" resultType="java.lang.String">
        SELECT DISTINCT perms
        FROM sys_menu
        WHERE status = '0'
          AND perms IS NOT NULL
          AND perms &lt;&gt; ''
    </select>
</mapper>
