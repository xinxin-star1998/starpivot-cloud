<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.org.starpivot.job.mapper.SysJobLogMapper">

    <resultMap type="cn.org.starpivot.job.domain.entity.SysJobLog" id="SysJobLogResult">
        <id    property="jobLogId"      column="job_log_id"    />
        <result property="jobName"     column="job_name"     />
        <result property="jobGroup"    column="job_group"   />
        <result property="invokeTarget" column="invoke_target" />
        <result property="jobMessage"  column="job_message" />
        <result property="status"      column="status"      />
        <result property="exceptionInfo" column="exception_info" />
        <result property="createTime"  column="create_time" />
    </resultMap>

    <sql id="selectJobLogVo">
        select job_log_id, job_name, job_group, invoke_target, job_message, status, exception_info, create_time from sys_job_log
    </sql>

    <select id="selectJobLogPage" resultMap="SysJobLogResult">
        <include refid="selectJobLogVo"/>
        <where>
            <if test="query.jobName != null and query.jobName != ''"> and job_name like concat('%', #{query.jobName}, '%')</if>
            <if test="query.jobGroup != null and query.jobGroup != ''"> and job_group = #{query.jobGroup}</if>
            <if test="query.status != null and query.status != ''"> and status = #{query.status}</if>
        </where>
        order by job_log_id desc
    </select>
</mapper>
