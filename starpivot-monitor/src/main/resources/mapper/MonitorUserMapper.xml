<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.org.starpivot.monitor.mapper.MonitorUserMapper">

    <select id="selectByUserIds" resultType="cn.org.starpivot.monitor.domain.entity.MonitorUser">
        SELECT user_id AS userId, user_name AS userName, nick_name AS nickName, dept_id AS deptId
        FROM sys_user
        WHERE user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND del_flag = '0'
    </select>

    <select id="selectByUserId" resultType="cn.org.starpivot.monitor.domain.entity.MonitorUser">
        SELECT user_id AS userId, user_name AS userName, nick_name AS nickName, dept_id AS deptId
        FROM sys_user
        WHERE user_id = #{userId}
          AND del_flag = '0'
    </select>

</mapper>
