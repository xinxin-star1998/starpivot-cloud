/**
 * Iconify 离线图标
 *
 * 安装：pnpm add -D @iconify-json/[prefix]
 * 使用：<ArtSvgIcon icon="heroicons-outline:home" />
 *
 * @module utils/ui/iconify-loader
 */

export {
  DEFAULT_ICON_SETS,
  ensureIconCollection,
  offlineIconLoaders,
  setupOfflineIconify,
  type OfflineIconSetOption
} from './iconify-offline'
